Pure React refers to writing React code without using JSX
const h1 = React.createElement(
"h1",
{
style: { color: "blue" },
},
"Welcome to Pure React"
);

     creating components and reusing a component
